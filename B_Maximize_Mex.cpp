#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(ll n, vector<ll> a, ll x) {
    
}

int main() {
    ll t;
    cin >> t;
    while(t--) {
        ll n, x;
        cin >> n >> x;
        vector<ll> a(n);
        for(auto& i : a)
            cin >> i;
        
        solve(n, a, x);
    }
}