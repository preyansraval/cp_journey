#include <bits/stdc++.h>
using namespace std;

void createStrings(string s, int N, int idx) {
    if(idx == N)
        return;
    
    for(int i = 0;i < N;i++) {
        if(i != idx) {

        }
    }
}

int main() {
    string s;
    cin >> s;

    createStrings(s, s.size(), 0);
}